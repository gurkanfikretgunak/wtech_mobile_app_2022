import 'package:flutter/material.dart';

class CustomUserProfile extends StatelessWidget {
  const CustomUserProfile({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: const CircleAvatar());
  }
}
